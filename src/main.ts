import { Plugin } from 'obsidian';

export default class FitKitPlugin extends Plugin {
  async onload(): Promise<void> {
    // Registrations will go here.
  }

  onunload(): void {
    // Teardown hooks will go here.
  }
}
